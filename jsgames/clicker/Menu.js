console.log('menu');
const s = ( sketch ) => {

  sketch.setup = () => {
    let canvas = sketch.createCanvas(window.innerWidth - (window.innerWidth / 3.5) - 10, window.innerHeight - (window.innerHeight / 12) - 10);
	frameRate(5000);
	console.log('setupmenu');
  };

  sketch.draw = () => {
    sketch.ASDASDZZZZZASD@$#@#$^#$%#$%